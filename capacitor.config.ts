import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.capacitor4startedluiggiplasencia0823',
  appName: 'ionic-capacitor4-started',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
